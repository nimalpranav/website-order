services:
  - type: web
    name: website order
    env: python
    buildCommand: "apt-get update && apt-get install -y cmake g++ make
      pip install -r requirements.txt"
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production

